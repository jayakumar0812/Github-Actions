# setup.py

from setuptools import setup, find_packages

setup(
    name="sample-python-app",
    version="1.0.0",
    description="A sample Python application to demonstrate GitHub Actions",
    author="Jay",
    packages=find_packages(),
    install_requires=[],
)
